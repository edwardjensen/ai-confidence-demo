name: Unpublish from Cloudflare Pages

on:
  schedule:
    # Run daily at 07:00 UTC
    - cron: '0 7 * * *'
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "confirm" to unpublish the site'
        required: true
        type: string

jobs:
  unpublish:
    runs-on: ubuntu-latest
    name: Unpublish AI Confidence Demo

    steps:
      - name: ✅ Validate manual trigger
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "confirm" ]; then
            echo "❌ Unpublish operation not confirmed. Please type 'confirm' to proceed."
            exit 1
          fi
          echo "✅ Manual unpublish confirmed"

      - name: 📁 Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: 🔧 Install Wrangler CLI
        run: npm install -g wrangler

      - name: 🗑️ Delete Cloudflare Pages deployment
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "🗑️ Deleting all deployments for project: ${{ secrets.CLOUDFLARE_PROJECT_NAME }}"
          
          # List deployments and extract IDs (without --format flag)
          echo "🔍 Listing deployments..."
          DEPLOYMENT_LIST=$(wrangler pages deployment list --project-name=${{ secrets.CLOUDFLARE_PROJECT_NAME }} 2>/dev/null || echo "")
          
          if [ -z "$DEPLOYMENT_LIST" ] || ! echo "$DEPLOYMENT_LIST" | grep -q "deployment"; then
            echo "ℹ️ No deployments found to delete"
          else
            echo "📋 Deployment list output:"
            echo "$DEPLOYMENT_LIST"
            
            # Extract deployment IDs from the output (assuming they're in the format of UUID-like strings)
            DEPLOYMENT_IDS=$(echo "$DEPLOYMENT_LIST" | grep -oE '[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}' || echo "")
            
            if [ -z "$DEPLOYMENT_IDS" ]; then
              echo "ℹ️ No deployment IDs found to delete"
            else
              echo "🔍 Found deployment IDs:"
              echo "$DEPLOYMENT_IDS"
              
              # Delete each deployment
              for deployment_id in $DEPLOYMENT_IDS; do
                echo "🗑️ Attempting to delete deployment: $deployment_id"
                if wrangler pages deployment delete $deployment_id --project-name=${{ secrets.CLOUDFLARE_PROJECT_NAME }} --force; then
                  echo "✅ Successfully deleted deployment: $deployment_id"
                else
                  echo "⚠️ Failed to delete deployment: $deployment_id"
                fi
              done
            fi
          fi
          
          echo "🏁 Deployment cleanup completed"

      - name: 📝 Create unpublish summary
        run: |
          echo "## 🗑️ Unpublish Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Project**: ${{ secrets.CLOUDFLARE_PROJECT_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Unpublished at**: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ github.event_name }}" = "schedule" ]; then
            echo "🕰️ **Reason**: Scheduled daily cleanup" >> $GITHUB_STEP_SUMMARY
          else
            echo "👤 **Reason**: Manual trigger" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Status**: Successfully unpublished from Cloudflare Pages" >> $GITHUB_STEP_SUMMARY

      - name: 📧 Notify on scheduled unpublish
        if: github.event_name == 'schedule'
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            
            // Create an issue to notify about the scheduled unpublish
            await github.rest.issues.create({
              owner,
              repo,
              title: `🗑️ AI Confidence Demo automatically unpublished - ${new Date().toISOString().split('T')[0]}`,
              body: `## Scheduled Unpublish Notification\n\nThe AI Confidence Demo has been automatically unpublished from Cloudflare Pages as part of the daily cleanup schedule.\n\n**Time**: ${new Date().toISOString()}\n**Trigger**: Scheduled cron job (07:00 UTC)\n\n---\n\nTo republish the demo, run the "Deploy to Cloudflare Pages" workflow manually.`,
              labels: ['automated', 'deployment']
            });